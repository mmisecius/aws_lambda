micronaut:
  application:
    name: graal
  config-client:
    enabled: false


aws:
  client:
    system-manager:
      parameterstore:
        enabled: true
        root-hierarchy-path: /e-invoicing/

